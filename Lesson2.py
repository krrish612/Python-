##input 
## here in where we took input from the user insted of hard coding everything
# input("...") shows the message and then waits for the user to type.
# The text the user types is stored in the variable "age".
age  = input("what is  Your age:")

# Show that value back to the user.
# Reminder: input() always returns a string, so age is text like "20", not the number 20.
print(age)
